#version 330

int i;
float bar(float x)
{
  float a=6.+x;
  x+=34.;
  float arr[2]=float[2](5.,float(float[2](7.,8.).length())),m=(a*=10,a*=20,a*=30,x++,arr[1]+arr[i++]);
  return a+x+m;
}
float baz(float a)
{
  a=a+4.+sin(a);
  a=a+5.+sin(a);
  return-a-a+a;
}
out vec3 outputvar;
void notMain(float x)
{
  outputvar.xyz=vec3(92)+vec3(bar(x)+baz(x));
}
