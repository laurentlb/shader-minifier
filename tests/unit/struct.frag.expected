#version 460

struct r{float dist;vec3 col;float spec;};
r c;
uniform MyInterfaceBlock{r e;};
r M()
{
  return c;
}
int M(r c)
{
  return 1;
}
int d()
{
  r c;
  c.dist=1.;
  c.spec=c.dist/2.;
  c=r(1.,vec3(1),c.spec);
  struct{r e;} k;
  for(r c;true;)
    break;
  for(struct{r i;} c;true;)
    break;
  return int(c.col.x+k.abc.dist);
}
struct{r k;} i;
