#define MIN_DIST 0.0015

#define MAX_DIST 64.0

#define MAX_STEPS 120.0

#define SHADOW_STEPS 30.0

float v,f,m,y,x,r,z,i,a;
struct A{
  float e;
  vec3 c;
  float v;
};
mat2 n(float v)
{
  float y=cos(v);
  v=sin(v);
  return mat2(y,v,-v,y);
}
float n(float v,float y,float m,float x,float f)
{
  return mix(x,f,clamp((v-y)/(m-y),0.,1.));
}
float n(vec3 v,vec3 m)
{
  v=abs(v)-m;
  return length(max(v,0.))+min(max(v.x,max(v.y,v.z)),0.);
}
float s(vec3 v,vec3 f)
{
  float m=n(v,f);
  v.xz*=n(.78525);
  f.xz*=-.18/1.41+1.41;
  return max(m,n(v,f));
}
float n(vec3 v)
{
  vec2 m=vec2(.1,.5);
  vec3 f=abs(v);
  return max(f.z-m.y,max(f.x*.866025+v.y*.5,-v.y)-m.x*.5);
}
float s(vec3 v)
{
  vec3 m=vec3(0,0,-.1),y=vec3(0);
  float x=dot(m-y,m-y),f=dot(v-y,m-y)/x,z=sqrt(dot(v-y,v-y)-f*f*x),s=max(0.,z-(f<.5?
    .25:
    .35)),c=abs(f-.5)-.5,a=clamp((.1*(z-.25)+f*x)/(.01+x),0.,1.);
  z=z-.25-a*.1;
  f-=a;
  return(z<0.&&c<0.?
    -1.:
    1.)*sqrt(min(s*s+c*c*x,z*z+f*f*x));
}
float n(vec3 v,float m,float f)
{
  vec2 a=abs(vec2(length(v.xy),v.z))-vec2(m,f);
  return min(max(a.x,a.y),0.)+length(max(a,0.));
}
float n(vec3 v,vec3 m,vec3 y)
{
  v-=m;
  m=y-m;
  return length(v-m*clamp(dot(v,m)/dot(m,m),0.,1.))-.2;
}
float n(vec2 v,float m)
{
  const vec3 f=vec3(-.9238795325,.3826834323,.4142135623);
  v=abs(v);
  v-=2.*min(dot(vec2(f),v),0.)*vec2(f);
  v-=2.*min(dot(vec2(-f.x,f.y),v),0.)*vec2(-f.x,f.y);
  v-=vec2(clamp(v.x,-f.z*m,f.z*m),m);
  return length(v)*sign(v.y);
}
vec3 n(vec3 v,vec3 f,vec2 m)
{
  v=normalize(f-v);
  f=normalize(cross(vec3(0,1,0),v));
  return normalize(v+f*m.x+cross(v,f)*m.y);
}
A n(A v,A y)
{
  return v.e<y.e?
    v:
    y;
}
void n(inout A v)
{
  v.c=vec3(.36,.45,.5);
  v.v=30.;
}
float s(float v)
{
  return sin(y*3.141*6.*v)*.2;
}
float n()
{
  return mix(5.,-2.,y);
}
float s()
{
  return abs(sin(z*3.1415*25.));
}
A s(vec3 v,float m,float f)
{
  f*=sin(v.x*150.)*sin(v.y*150.)*.002;
  A r;
  r.e=n(v,vec3(1,m,2));
  r.e=max(mix(r.e,(length(v/vec3(1,.8,1))-1.)*.8,.57),-v.y)-f;
  r.c=vec3(.05);
  r.v=30.;
  return r;
}
A p(vec3 v)
{
  vec3 m=v;
  A r=s(v*vec3(.95,-1.4,.95),1.,0.);
  float i=max(max(s((v+vec3(0,.01,0))*vec3(.95),1.,0.).e,v.y-.35),v.y*.625-v.z-.66);
  r.e=min(r.e,i);
  v.xy*=n(.075*(f-1.)*sign(v.x));
  v.x=abs(v.x)-1.33;
  v.y-=.1-v.x*.1;
  r.e=min(r.e,n(v,vec3(.4,.06*(1.-v.x),.3-v.x*.2)));
  v=m;
  v.y=abs(abs(v.y+.147)-.0556)-.0278;
  r.e=max(r.e,-n(v+vec3(0,0,1.5),vec3(mix(.25,.55,-m.y),.015,.1)));
  v=m;
  v.y=abs(v.y+.16)-.06;
  v.z-=-1.1;
  i=max(max(n(v.xzy,1.,.03),-n(v.xzy,.55,1.)),v.z+.2);
  r.e=max(r.e,-i);
  n(r);
  return r;
}
A t(vec3 v)
{
  A f;
  n(f);
  v.yz+=vec2(.1,.45);
  vec3 m=v;
  m.z=abs(m.z)-.5;
  f.e=s(m);
  f.e=min(f.e,n(v,.35,.4));
  m=vec3(v.x,.28-v.y,v.z);
  f.e=min(f.e,n(m));
  m=v;
  m.x=abs(v.x);
  m.xy*=n(.78525);
  float r=sign(sin(m.z*33.3))*.003;
  r=n(m,vec3(.1-r,.38-r,.34))-.02;
  m=v-vec3(0,0,-.6);
  m.x=abs(m.x)-.1;
  r=min(min(min(r,n(m,.06,.15)),n(m+vec3(0,.12,-.05),.06,.05)),n(v+vec3(0,0,.54),vec3(.1,.06,.04)));
  if(r<f.e)
    r=max(r,-n(m+vec3(0,0,.1),.03,.2)),f.e=r,f.c=vec3(.02);
  float i=s();
  if(i>.5)
    {
      r=n(m,.01+m.z*.05,fract(i*3322.423)*.5+.9);
      if(r<f.e)
        f.e=r,f.c=vec3(1),a+=.1/(.01+r*r*4e2);
    }
  return f;
}
A h(vec3 v)
{
  A r;
  n(r);
  v.x=abs(v.x);
  v.yz+=vec2(.24,0);
  v.xy*=n(.15*(f-1.));
  const vec3 y=vec3(1.5,0,0),a=y-vec3(0,0,.3);
  r.e=min(n(v,vec3(0),y),n(v,y,a));
  v-=a;
  v.z-=m*.15;
  return n(r,t(v));
}
float c(vec3 v)
{
  v.yz+=vec2(.1,.32);
  return max(n(v,vec3(.3+.2*(v.z-.18)-v.y*.456*.5,.3+.2*cos((v.z-.18)*3.69),.35)),.1-v.y);
}
float e(vec3 v)
{
  v.z+=.8;
  v.yz*=n(.86);
  float m=c(v);
  v.xz*=n(1.57);
  v.x-=.43;
  v.z=.25-abs(v.z);
  return min(m,c(v));
}
A d(vec3 v)
{
  A f;
  n(f);
  v.y+=.65;
  v.yz*=n(-.2);
  float m=s(1.);
  v.xy*=n(m*.3);
  vec3 r=v;
  r.y+=.3;
  f.e=max(n(r.zyx,.5,.5),v.y+.15);
  float i=.5-abs(sin(v.y*40.))*.03,a=n(v,vec3(i,.15,i));
  i=.3-abs(sin(v.x*40.))*.03;
  r.y+=.18;
  a=min(a,n(r.zyx,i,.75));
  r.x=abs(r.x);
  r.yz*=n(-.58525+m*sign(v.x));
  r.x-=.98;
  f.e=min(f.e,max(n(r.zyx,.4,.24),-r.y));
  f.e=min(f.e,n(r,vec3(.24,.2,.14+.2*r.y)));
  v=r;
  r.xz=abs(r.xz)-vec2(.12,.25);
  i=min(n(r.xzy,.1,.325),n(r.xzy,.05,.5));
  f.e=min(f.e,max(i,r.y));
  v.y+=.68;
  f.e=min(f.e,n(v,vec3(sign(abs(v.y)-.04)*.005+.26,.2,.34)));
  if(a<f.e)
    f.e=a,f.c=vec3(.02);
  return f;
}
A l(vec3 v)
{
  A f;
  n(f);
  float m=s(1.);
  v.z+=.27;
  v.yz*=n(m*sign(v.x));
  v.z-=.27;
  v.y+=.65;
  v.yz*=n(-.2);
  v.xy*=n(m*.3);
  vec3 r=v;
  r.x=abs(r.x);
  r.y+=.48;
  r.yz*=n(-.58525);
  r.x-=.98;
  vec3 i=r;
  v=r;
  r.xz=abs(r.xz)-vec2(.12,.25);
  v.y+=.68;
  v.xy=abs(v.xy)-.12;
  m=n(v,vec3(.07,.05,1.2));
  i-=vec3(0,-.7,0);
  f.e=n(i-vec3(0,0,1.15),vec3(.17,.17,.07))-.04;
  i.z+=1.;
  f.e=min(f.e,s(i.xzy,vec2(.28-sign(abs(i.z)-.3)*.01,.5).xyx));
  f.e=min(f.e,e(i));
  if(m<f.e)
    f.e=m,f.c=vec3(.8);
  return f;
}
A M(vec3 y)
{
  y.yz+=vec2(s(2.)*.2+.1,-n());
  A i=l(y);
  float a=min(v*2.,1.);
  y.yz-=(a<.5?
    smoothstep(0.,.5,a):
    1.-smoothstep(.5,1.,a)*.2)*.5;
  f=smoothstep(0.,1.,clamp((v-.66)*6.,0.,1.));
  m=smoothstep(0.,1.,clamp((v-.83)*6.,0.,1.))+s()*.5;
  i=n(i,d(y));
  y.yz*=n(.1*(-r+s(2.)+smoothstep(0.,1.,clamp((v-.5)*6.,0.,1.))-1.));
  y.xz*=n(x*.2);
  i=n(i,p(y));
  i=n(i,s(y,.8,1.));
  return n(i,h(y));
}
A w(vec3 v)
{
  A f;
  f.c=vec3(.4);
  f.v=1e7;
  vec3 m=vec3(2.8,2.6,.1);
  vec2 r=v.xy-vec2(0,2);
  v.x=abs(v.x);
  v.yz+=vec2(.5,-3.4);
  float a=n(v,m+vec3(0,0,1)),s=length(v.z-8.);
  f.e=min(s,max(length(v.z),-a+.1));
  if(f.e==s)
    if(min(max(min(abs(n(r,2.6)),abs(n(r,1.9))),min(.7-abs(r.x+1.2),-r.y)),max(abs(n(r,1.2)),min(r.x,.7-abs(r.y))))<.3)
      f.c=vec3(.39,.57,.71);
  a=max(n(v,m+vec3(.4,.4,.1)),-a);
  s=m.x*.5;
  v.x-=m.x;
  v.xz*=n(i*2.1);
  v.x+=s;
  float c=n(v,vec3(s,m.yz));
  v=abs(v)-vec3(s*.5,1.1,.14);
  c=min(a,max(c,-max(n(v,vec3(.45,.9,.1)),-n(v,vec3(.35,.8,1)))));
  if(c<f.e)
    f.e=c,f.c=vec3(.02,.02,.024),f.v=10.;
  return f;
}
A S(vec3 v)
{
  A f=n(w(v),M(v));
  float m=length(v.y+3.);
  if(m<f.e)
    f.e=m,f.c=vec3(.1);
  return f;
}
float M(vec3 v,vec3 m)
{
  m=normalize(m-v);
  float r=1.,f=.1;
  for(float i=0.;i<SHADOW_STEPS;i++)
    {
      float a=S(v+m*f).e;
      r=min(r,12.*a/f);
      f+=a;
      if(r<.001||f>25.)
        break;
    }
  return clamp(r,0.,1.);
}
vec3 M(vec3 v,float y)
{
  vec2 f=vec2(1,-1)*.5773*(.01*y*.33);
  return normalize(f.xyy*S(v+f.xyy).e+f.yyx*S(v+f.yyx).e+f.yxy*S(v+f.yxy).e+f.xxx*S(v+f.xxx).e);
}
vec3 M(vec3 v,vec2 m)
{
  m=m.xy/iResolution.xy;
  return v*(.5+.5*pow(16.*m.x*m.y*(1.-m.x)*(1.-m.y),.4));
}
vec3 M(vec3 v,vec3 m,float f,A r)
{
  const vec3 y=vec3(10,10,-10);
  vec3 i=normalize(y-v),x=M(v,f);
  f=max(0.,dot(i,x));
  f*=mix(.2,1.,M(v,y));
  i=((f+max(0.,dot(-i,x))*.2)*(S(v+.33*x).e/.33)+pow(max(0.,dot(m,reflect(i,x))),r.v)*2.)*vec3(2,1.6,1.7);
  return mix(r.c*i,vec3(.01),smoothstep(.7,1.,1.+dot(m,x)))*exp(-length(v)*.05);
}
vec3 S(vec3 v,vec3 m)
{
  vec3 f;
  float r=.01;
  A y;
  for(float i=0.;i<MAX_STEPS;i++)
    {
      f=v+m*r;
      y=S(f);
      if(abs(y.e)<MIN_DIST*r)
        break;
      if(r>MAX_DIST)
        return vec3(0);
      r+=y.e;
    }
  float i=a;
  return M(f,m,r,y)+s()*.3+i;
}
void mainImage(out vec4 m,vec2 f)
{
  y=1.;
  x=0.;
  r=0.;
  z=0.;
  i=1.;
  v=1.;
  vec3 a,c;
  float l,d,e=mod(iTime,55.);
  if(e<12.)
    l=0.,d=12.,y=0.,a=vec3(0,-1.5,-.625),c=vec3(0,-1,n()),i=smoothstep(0.,1.,e/5.),v=n(e,7.,10.,0.,1.);
  else if(e<25.)
    {
      l=12.;
      d=25.;
      float v=e-l;
      y=smoothstep(0.,1.,n(v,3.,8.,0.,1.));
      a=vec3(-.5*cos(v*.7),.5-v*.1,n()-3.);
      c=vec3(0,0,n());
    }
  else if(e<29.)
    {
      l=25.;
      d=29.;
      float v=e-l;
      a=vec3(-2,.5+v*.1,n()-3.);
      c=vec3(0,0,n());
    }
  else if(e<37.)
    {
      l=29.;
      d=37.;
      float m=e-l;
      a=vec3(1.5,-1.-m*.05,n()-5.);
      c=vec3(0,-1,n());
      v=n(m,2.,5.,1.,0.);
    }
  else if(e<55.)
    {
      l=37.;
      d=55.;
      float m=e-l;
      a=vec3(-1.8,-.5,n()-2.5);
      v=n(m,2.,3.,0.,1.)-n(m,11.5,14.5,0.,1.);
      c=vec3(0,v*.5-.5,n());
      x=n(m,3.,3.2,0.,1.)*v;
      r=n(m,3.2,3.4,0.,1.)*v;
      z=m<=9.5?
        n(m,4.,9.5,0.,1.):
        0.;
    }
  float A=1.-cos(min(1.,2.*min(abs(e-l),abs(e-d)))*3.141/2.);
  vec3 p=vec3(0);
  
#ifdef AA

  for(float v=0.;v<=1.;v++)
    for(float m=0.;m<=1.;m++)
      {
        vec2 i=f+vec2(v,m)*.5;
        
#else

        vec2 r=f;
        
#endif

        r+=(fract(s()*vec2(23242.2323,978.23465))-.5)*10.;
        vec2 x=(r-.5*iResolution.xy)/iResolution.y;
        p+=S(a,n(a,c,x));
        
#ifdef AA

      }
  p/=4.;
  
#endif

  m=vec4(M(pow(p*A,vec3(.4545)),f),1);
}
